To use it, stor it in the location of the movie.json, then search in the file "main .js" (it can have a hash in the name) for "movie.json" and add the .php extension


Also Possible: rename it to index.php and store it in a folder named movie.json